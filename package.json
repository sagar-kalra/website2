{
  "name": "pclub-website",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "dependencies": {
    "@material-ui/core": "^3.9.1"
  }
}
